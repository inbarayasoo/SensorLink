#pragma once

// Single place the protocol library states its own version string.
// Used for now only to give the smoke test something real to include.
namespace proto {

inline constexpr const char* kVersion = "0.1.0";

}  // namespace proto
